name: mazes
channels:
  - conda-forge
  - nodefaults
dependencies:
  # C++ Toolchain & Build System
  - cxx-compiler
  - cmake >=3.25
  - make

  # Libraries for Image Processing & Compression
  - libpng
  - zlib

  # OpenCL Vendor-Agnostic GPU/CPU Development
  - ocl-icd
  - opencl-headers
  - pocl
